/**
 * The options to pass to `Callable`'s constructor.
 *
 * @typedef {Object} CallableOptions
 *
 * @property {string | null} name
 * The function name of the `Callable`.
 */
export default interface CallableOptions {
    name: string | null
}
